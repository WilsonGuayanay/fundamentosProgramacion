// Strings --> Cadena de caracteres
export let fristName = 'Wilson';
let lastName: string = 'Alexander';

// console.log( fristName, lastName );
let fullName: string = fristName + ' ' + lastName;
console.log(fullName)

let number1: number = 10.5;
let number2: number = 20.3;
console.log(number1 + number2);

// Data boolean
let isActive: boolean = true;
let isRunning: boolean = false;
let hasKids: boolean = true;

// Se niega el primer valor
isActive = !isActive;

console.log('isActive:', isActive);
console.log('isRunning:', isRunning);
console.log('hasKids:', hasKids);



